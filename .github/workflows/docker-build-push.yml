name: Docker Build and Push

on:
  push:
    branches: ["main"]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.manojd710 }}
          password: ${{ secrets.dckr_pat_os4HWtfMWxKU-joDbDpciuyzZaA }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.manojd710 }}/fragments:latest
            ${{ secrets.manojd710 }}/fragments:${{ github.sha }}
